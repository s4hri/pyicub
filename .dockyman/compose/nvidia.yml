version: '3.8'

services:

  base:

    extends:
      file: base.yml
      service: base

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [graphics,utility]
